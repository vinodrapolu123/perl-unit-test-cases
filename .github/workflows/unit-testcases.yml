name: Perl Unit test workflow
on: push
jobs:
    unit_test:
        runs-on: ubuntu-latest
        steps:
            - name: Get Code
              uses: actions/checkout@v3
            - name: Run Unit tests
              run: |
                    sudo apt update -y
                    sudo apt install cpan
                    sudo cpan install Test::Fatal
                    prove -v